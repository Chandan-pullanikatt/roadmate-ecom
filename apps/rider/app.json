{
  "expo": {
    "_comment_single_listing": "RoadMate Rider is ONE Play Store listing, unlike apps/business's four. A shop's own delivery boy and a RoadMate delivery partner install the same app — the delivery flow is identical and only the money differs (HANDOFF §3, §4). So there is no app.config.js here and no APP_VARIANT: this file is the whole configuration.",
    "name": "RoadMate Rider",
    "slug": "roadmate-rider",
    "scheme": "roadmate-rider",
    "version": "1.0.0",
    "orientation": "portrait",
    "icon": "./assets/icon.png",
    "userInterfaceStyle": "light",
    "newArchEnabled": true,
    "splash": {
      "backgroundColor": "#DEBE10"
    },
    "ios": {
      "supportsTablet": false,
      "bundleIdentifier": "com.roadmate.rider",
      "infoPlist": {
        "UIBackgroundModes": [
          "location"
        ]
      }
    },
    "android": {
      "package": "com.roadmate.rider",
      "permissions": [
        "android.permission.ACCESS_COARSE_LOCATION",
        "android.permission.ACCESS_FINE_LOCATION"
      ],
      "adaptiveIcon": {
        "backgroundColor": "#DEBE10",
        "foregroundImage": "./assets/android-icon-foreground.png",
        "backgroundImage": "./assets/android-icon-background.png",
        "monochromeImage": "./assets/android-icon-monochrome.png"
      }
    },
    "web": {
      "favicon": "./assets/favicon.png"
    },
    "plugins": [
      "expo-router",
      "expo-secure-store",
      [
        "expo-location",
        {
          "_comment": "The wording a rider actually reads at the permission prompt. It has to say what the location is FOR — it is not analytics, it is what decides whether they are offered a job at all.",
          "locationAlwaysAndWhenInUsePermission": "RoadMate Rider uses your location while you are on shift so orders near you can be assigned to you, and so the customer can see you coming.",
          "locationWhenInUsePermission": "RoadMate Rider uses your location while you are on shift so orders near you can be assigned to you, and so the customer can see you coming."
        }
      ],
      [
        "expo-image-picker",
        {
          "_comment": "Proof of delivery (2026-08-09). The prompt says what the photo is for and that it is optional, because it is — the delivery code is what completes an order, and a refused camera must never make one undeliverable.",
          "photosPermission": "RoadMate Rider can attach a photo to a delivery as proof it was handed over. This is optional.",
          "cameraPermission": "RoadMate Rider uses the camera to photograph a completed delivery as proof it was handed over. This is optional.",
          "_comment_microphone": "microphonePermission: false is load-bearing, not tidying. The plugin adds android.permission.RECORD_AUDIO by default, because the picker CAN record video — this app never does: every call sites mediaTypes as ['images'] only. Left in, Play Store lists Microphone against a delivery app and the Data Safety form has to justify a capability that is never used. EAS re-adds it on every prebuild unless this says otherwise.",
          "microphonePermission": false
        }
      ]
    ],
    "extra": {
      "_comment_apiUrl": "Overridden per environment. A phone cannot reach the API on localhost — this must be the dev machine's LAN address (see .env.example).",
      "apiUrl": "http://localhost:5000",
      "router": {},
      "eas": {
        "projectId": "5d3a3c1b-b5e1-46e8-b5d9-c9645efd4186"
      }
    },
    "_comment_icons": "Icons are the client's actual wordmark (2026-08-12), the same mark BrandMark shows on sign-in — NOT the \"R\" monogram that shipped on 2026-08-11. The monogram was a stand-in and read as a generic letter tile on the launcher. Generated from packages/ui/assets/roadmate-logo.jpeg: the black wordmark is keyed out of the photograph's yellow field by luminance and re-laid on colors.accent (#DEBE10), so the icon, the adaptive background and the splash are all the one token rather than the JPEG's #D8BE11. The adaptive foreground is deliberately narrower than icon.png (640 vs 800 px on a 1024 canvas) because Android crops to a ~66% circular safe zone and a 6.4:1 wordmark inscribed in that circle maxes out near 675 px — any wider and the \"®\" is cut off by a round OEM mask. Still outstanding: STORE SCREENSHOTS, and a real vector/square export from the client — this is a ~1.5x upscale of a 529x83 px region, which is as sharp as the supplied 1280x720 photograph allows."
  }
}
